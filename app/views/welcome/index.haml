

%h1 Public Data

.public-homes
  %table.pure-table
    %thead
      %tr
        %th.sortable
          Name
    %tbody
      - @public_homes.each do |home|
        %tr
          %td
            = link_to home.name, home_path(home)