= form_for @home, url: { action: 'create' }, class: 'pure-form' do |f|
  %section.card
    %headers
      %h2 New whare to monitor
    = render 'form', f: f, home_types: @home_types
    %footer
      %button.btn.btn-primary{ type: 'submit' } Create
