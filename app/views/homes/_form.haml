/ %input{type: "name", placeholder: "Name of home"}
%fieldset
  = f.text_field :name, type: 'name', placeholder: 'Name of home'

%fieldset
  - home_types.each do |ht|
    %p
      = f.radio_button :home_type_id, ht.id
      = f.label :home_type_id, ht.name

%fieldset
  = f.label :is_public
  = f.check_box :is_public, label: 'Public?'
