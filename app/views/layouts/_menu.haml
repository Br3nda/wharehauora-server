.nav
  - if user_signed_in? && @my_homes.size.positive?
    %ul.nav-links
      %li
        = link_to home_rooms_path(@my_homes.first) do
          Dashboard
    %h6 Rooms
    %ul.nav-links
      - @my_homes.first.rooms.order(:name).each do |room|
        %li
          = link_to room do
            = room.name
          -# %a.current{href: "./room-detail1.html"} Living Room
    -# %h6 Setup
    -# %ul.nav-links
    -#   %li
    -#     %a{href: "./rooms.html"} Whare  Rooms
    -#   %li
    -#     %a{href: "./whanau.html"} Whānau
    %h6 Account
    %ul.nav-links
      %li
        = link_to edit_user_registration_path do
          My Account
      -# %li
      -#   %a{href: "./help.html"} Help
      %li
        = link_to destroy_user_session_path, method: :delete do
          Logout
    -if current_user.role?('janitor')
      TODO admin menu
      / = render 'menus/admin'
  - else
    %ul.nav-links
      %li
        = link_to 'Sign up', new_user_registration_path
      %li
        = link_to 'Sign in', new_user_session_path
