%nav.menu
  %header
    %a.logo{href: "./"} Whare Hauora
    %h6 Menu
    %a.x{href: "#"}
      %img.svg{alt: "close", src: image_tag(image_url('icon/x.svg'))}/
  .nav
    - if user_signed_in?
      - current_user.homes.each do |home|
        %ul.nav-links
          %li
            = link_to 'Dashboard', home_rooms_path(home), class: 'current'

        %h6 Rooms
        
        %ul.nav-links
          - home.rooms.each do |room|
            %li
              = link_to room_path(room) do
                = room.name
        %h6 Setup
        %ul.nav-links
          -# %li
          -#   %a{href: "./rooms.html"} Whare  Rooms
          %li
            = link_to home_home_viewers_path(home) do
              Whānau
      %h6 Account
      %ul.nav-links
        %li
          = link_to 'Change password', edit_user_registration_path
        -# %li
        -#   %a{href: "./help.html"} Help
        %li
        = link_to 'Logout', destroy_user_session_path, method: :delete
    - else
      %ul.nav-links
        %li
          = link_to 'Sign up', new_user_registration_path
        %li
          = link_to 'Sign in', new_user_session_path
  -# %footer
  -#   %select.lang-picker
  -#     %option{"data-iconurl" => "/img/icon/lang-eng.svg", value: "English"} English
  -#     %option{"data-iconurl" => "/img/icon/lang-mri.svg", value: "Te Reo Māori"} Te Reo M&#x101;ori
  -#     %option{"data-iconurl" => "/img/icon/lang-smo.svg", value: "Samoan"} Samoan