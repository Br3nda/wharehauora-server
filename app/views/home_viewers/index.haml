%h2
  = @home.name
= render 'homes/nav', home: @home

%h3 Whānau

.pure-menu.pure-menu-horizontal
  %ul.pure-menu-list
    %li.pure-menu-item
      = link_to 'add someone to whare', new_home_home_viewer_path(@home), class: 'pure-menu-link'

- if @home.users.present?
  %h3 Users
  %table.pure-table
    %thead
      %tr
        %th.sortable
          Email
    %tbody
      - @home.users.each do |v|
        %tr
          %td
            = v.email
          %td
            delete
- else
  %p No whānau

