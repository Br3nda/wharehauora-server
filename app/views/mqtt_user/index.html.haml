= render 'homes/nav', home: @home
%h2 DEBUG

- if @home.mqtt_user
  %p
    Here is the user+pass for your gateway.
    = @home.name

  %p
    Username:
    %b= @home.mqtt_user.username
  %p
    Password:
    %b= @home.mqtt_user.password
- else
  %p Gateway mqtt user has not been provisioned yet


- if @home.gateway_mac_address.blank?
  %p Cannot provision home with no mac address set


- if @gateway.present?
  %p
    Gateway last known reboot/connection:
    %b= @gateway.updated_at.localtime
  %p
    Gateway software version:
    %b= @gateway.version
